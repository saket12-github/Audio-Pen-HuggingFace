# Gradio (HF Spaces SDK pins may override — avoid unsupported component kwargs)
gradio>=4.44.0,<5.0.0
# Transcription (CTranslate2 backend — no PyTorch needed for Whisper)
faster-whisper>=1.0.3,<2.0.0
# Together AI (OpenAI-compatible HTTP API)
httpx>=0.27.0,<1.0.0
numpy>=1.24.0
